{
    "replacementPatterns": [
        {
            // adding this so relative links don't fail
            "pattern": "^/",
            "replacement": "http://astrobiomike.github.io/"
        }
    ]
}
